name: Build Docker

on:
  push:
    branches: [ master ]
    tags:
      - 'v*'
    paths:
    - "**"
    # Don't build when it is just a doc update
    - '!docs/**'
    - '!wiki/**'
    - '!*.md"

jobs:

  job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: run
        run: |
          echo "I ran!"
